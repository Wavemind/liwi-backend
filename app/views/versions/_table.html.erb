<table class="table table-condensed table-hover" id="versions-datatable" data-source="<%= algorithm_versions_url(@algorithm, format: :json, id: @algorithm.id) %>">
  <thead>
    <tr>
      <th><%= t('.columns.name') %></th>
      <th><%= t('.columns.last_update') %></th>
      <th><%= t('.columns.creator') %></th>
      <th><%= t('.columns.actions') %></th>
    </tr>
  </thead>
  <tbody></tbody>
</table>
