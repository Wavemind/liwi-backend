<table class="table table-condensed table-hover" id="relations-datatable" data-source="<%= algorithm_algorithm_version_relationable_url(@algorithm.id, @algorithm_version.id, @relationable.class, @relationable.id, format: :json) %>">
  <thead>
    <tr>
      <th><%= t('.columns.id') %></th>
      <th><%= t('.columns.reference') %></th>
      <th><%= t('.columns.type') %></th>
      <th><%= t('.columns.label') %></th>
      <th><%= t('.columns.children') %></th>
      <th><%= t('.columns.actions') %></th>
    </tr>
  </thead>
  <tbody></tbody>
</table>

