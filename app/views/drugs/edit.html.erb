<%= render 'utils/dependency_warning', node: @drug %>

<div class="mx-0 my-0 metadata" data-type="Drug" data-algorithm="<%= @algorithm.id %>" data-question="<%= @drug.id %>"></div>

<div class="row justify-content-center mt-5">
  <div class="col-md-8">
    <%= react_component('form/DrugForm/stepper', { drug: @drug.as_json(include: [:formulations]), method: 'update', from: 'rails', is_deployed: @drug.used_in_deployed_version }) %>
  </div>
</div>
