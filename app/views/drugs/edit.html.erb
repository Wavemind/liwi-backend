<h1><%= t('.title') %></h1>

<%= render 'utils/dependency_warning', node: @drug %>

<div class="row justify-content-center">
  <%= simple_form_for @drug, url: algorithm_drug_url(@algorithm, @drug), html: { class: 'form-horizontal' }, wrapper: :input_group do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
