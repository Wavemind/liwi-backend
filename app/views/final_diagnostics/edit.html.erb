<h1><%= t('.title') %></h1>

<%= render 'utils/dependency_warning', node: @final_diagnostic %>

<div class="row justify-content-center mt-5">
  <div class="col-md-8">
    <%= react_component('form/FinalDiagnosticForm', { finalDiagnostic: @final_diagnostic, method: 'update' }) %>
  </div>
</div>
