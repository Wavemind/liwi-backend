<h1><%= t('.title') %></h1>

<div class="row justify-content-center">
  <%= simple_form_for @question.becomes(Question), url: algorithm_questions_url(@algorithm), html: { class: 'form-horizontal' }, wrapper: :input_group do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
