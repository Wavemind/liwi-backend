<h1><%= t('.title') %></h1>

<div class="row justify-content-center">
  <%= simple_form_for @user, html: { class: 'form-horizontal' }, url: generate_users_url do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
