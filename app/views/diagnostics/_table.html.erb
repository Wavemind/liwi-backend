<table class="table table-condensed table-hover" id="diagnostics-datatable" data-source="<%= diagnostics_url(format: :json, id: AlgorithmVersion.first) %>">
  <thead>
    <tr>
      <th><%= t('.columns.reference') %></th>
      <th><%= t('.columns.label') %></th>
      <th><%= t('.columns.last_update') %></th>
      <th><%= t('.columns.actions') %></th>
    </tr>
  </thead>
  <tbody></tbody>
</table>

