<%= simple_form_for [@algorithm, @algorithm_version, @diagnostic], html: { class: 'form-horizontal' } do |f| %>

  <%= f.input :reference %>
  <%= f.input :label %>

  <%= f.submit t('save'), class: 'btn btn-primary my-4 float-right' %>
<% end %>
