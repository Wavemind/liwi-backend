<div class="nested-fields">
  <div class="row">
    <%= f.input :reference, prepend: "#{@question.reference}_" %>
    <%= f.input :value %>
    <%= f.input :operator %>

    <%= link_to_remove_association "<button class='btn btn-outline-danger'>#{t('remove')}</button".html_safe, f %>
  </div>
</div>
