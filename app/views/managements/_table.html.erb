<h3><%= t('algorithms.show.subtitle_managements') %></h3>

<%= link_to new_algorithm_management_url(@algorithm) do %>
  <button type="button" class="btn btn-outline-primary"><%= t('add') %></button>
<% end %>

<table class="table table-condensed table-hover" id="managements-datatable" data-source="<%= management_algorithm_url(@algorithm, format: :json) %>">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= t('.columns.reference') %></th>
      <th><%= t('.columns.label') %></th>
      <th><%= t('.columns.description') %></th>
      <th><%= t('.columns.actions') %></th>
    </tr>
  </thead>
  <tbody></tbody>
</table>
