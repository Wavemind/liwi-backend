<h1><%= t('.title') %></h1>

<%= render 'utils/dependency_warning', node: @management %>

<div class="row justify-content-center">
  <%= simple_form_for @management, url: algorithm_management_url(@algorithm, @management), html: { class: 'form-horizontal' }, wrapper: :input_group do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
