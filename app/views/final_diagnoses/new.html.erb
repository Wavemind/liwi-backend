<h1><%= t('.title') %></h1>

<div class="mx-0 my-0 metadata" data-type="<%= @diagnosis.class.name %>" data-id="<%= @diagnosis.id %>" data-version="<%= @diagnosis.version.id %>" data-algorithm="<%= @diagnosis.version.algorithm.id %>" data-final_diagnosis="<%= @final_diagnosis.id %>"></div>

<div class="row justify-content-center mt-5">
  <div class="col-md-8">
    <%= react_component('form/FinalDiagnosisForm', { finalDiagnosis: @final_diagnosis, method: 'create', from: 'rails' }) %>
  </div>
</div>
