<%= simple_form_for @study, html: { class: 'form-horizontal' } do |f| %>

  <%= f.input :label %>
  <%= f.association :algorithm, collection: Algorithm.all, label_method: :name, prompt: t('select')  %>

  <%= f.submit t('save'), class: 'btn btn-primary my-4 float-right' %>
<% end %>
